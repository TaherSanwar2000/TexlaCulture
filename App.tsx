import {NavigationContainer} from '@react-navigation/native';
import Navigator from './Root Navigator/Navigator';

const App = () => {
  return (
    <NavigationContainer>
      <Navigator />
    </NavigationContainer>
  );
};

export default App;
